const logName = (name) => {
  console.log(name)
}

const sum = (a, b) => {
  return a + b
}

const divide = (a, b) => {
  return a / b
}

let fname = 'Dư'

export { sum, divide }

export default logName
